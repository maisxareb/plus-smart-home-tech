spring:
  application:
    name: shopping-cart
  config:
    import: "configserver:"

  cloud:
    config:
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/